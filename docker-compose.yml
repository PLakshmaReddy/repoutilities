version: '3.8'

services:
  app:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - AWS_REGION=us-east-1
    volumes:
      - ./repos:/app/repos
      - ./config.json:/app/config.json
